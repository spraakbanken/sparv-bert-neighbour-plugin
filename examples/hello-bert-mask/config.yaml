metadata:
    id: hello-bert-mask
    language: swe

import:
    importer: text_import:parse

export:
    annotations:
        - <sentence>
        - <token:word>
        - <token>:stanza.pos
        - <token>:word_prediction:word-prediction

sparv:
    compression: none
